# NUnit.ApplicationDomain

This is a fork of zastrowm/NUnit.ApplicationDomain ported to NUnit 4.3.1 and .NET Framewok 4.8.1.
Note: Platform is x64.

## .NET 8/9
There is partial support for .NET 8 and .NET 9.

Not supported:

+ `app.config` is not copied over AppDomain.
